TODO

- newWizards: Post bug to Eclipse, as schema is invalid
- Markus:
	- lejos ist jetzt auch ein plugin
	- org.lejos: jtools.jar rausgenommen, jetzt in lejos
	- andere Plugins: Re-Export von leJOS
	- lejos: Patches in js.tinyvm aufgenommen
	- Anforderung:
		- Abstraktion von Writer (System.out, und writer mit verschiedenen Kategorien 
		  und/oder Leveln), und von Assertion: In Toolset Meldung, aber kein Exit, und
		  Cmdline tools mit Meldung auf System.out/err und Exit.
	- Code Conventions:
		- Java Defaults, Tab size = 4, use spaces, auto format, ...

- fix all deprecated warnings

- New project wizard: Add leJOS default jars to CLASSPATH
- Fix Compile/Link for Eclipse RC2
- Create site based deployment, to support feature based deployment
- add LDT branding

- Sort out the docs/internal into the new docs plugin TOC
- write a user guide explaining the steps how to work with LDT
